import React from 'react';
import { SideBar } from '../../components/organisms';

const Home = () => {

    return (
        <div>
            <SideBar />
        </div>
    );
};

export default Home;