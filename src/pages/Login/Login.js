import React from 'react';
import { Authentification } from '../../components/organisms';

const Login = () => {
    return (
        <div>
            <Authentification />
        </div>
    );
};

export default Login;